#ifndef PYTRA_CORE_TAGGED_VALUE_H
#define PYTRA_CORE_TAGGED_VALUE_H

// Legacy header — PyBoxed/py_box/py_unbox removed.
// Object<T> + ControlBlock + PyBoxedValue<T> (in object.h) replace all use cases.
// This empty header is kept for backward compatibility with generated .cpp files
// that may still #include it.

#endif  // PYTRA_CORE_TAGGED_VALUE_H
